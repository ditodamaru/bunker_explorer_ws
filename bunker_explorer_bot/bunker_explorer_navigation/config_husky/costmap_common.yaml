footprint: [[-0.5, -0.33], [-0.5, 0.33], [0.5, 0.33], [0.5, -0.33]]
footprint_padding: 0.01

#robot_base_frame: base_footprint #original
robot_base_frame: robot_base
update_frequency: 4.0
publish_frequency: 3.0
transform_tolerance: 0.5

resolution: 0.05

obstacle_range: 5.5
raytrace_range: 6.0

#layer definitions original
#static:
#    map_topic: /map
#    subscribe_to_updates: true

#original
#obstacles_laser:
#    observation_sources: laser
#    laser: {data_type: LaserScan, clearing: true, marking: true, topic: scan, inf_is_valid: true}

inflation:
    inflation_radius: 1.0

map_topic: /rtabmap/grid_map
subscribe_to_updates: true

observation_sources: point_cloud_sensor

point_cloud_sensor: {sensor_frame: camera_link, data_type: PointCloud2, topic: /point_cloud_data, marking: true, clearing: true}